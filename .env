# Database Configuration
# Option A: If you have PostgreSQL running on the host machine
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/paste

# Option B: If you have PostgreSQL in another container (replace 'postgres-container-name' with actual name)
# DATABASE_URL=postgresql://postgres:postgres@postgres-container-name:5432/paste

# Server Configuration
BIND_ADDRESS=0.0.0.0:3000

# Default redirect URL (when accessing root /)
DEFAULT_REDIRECT_URL=https://starpaste.eu/

# Optional: Rust log level
RUST_LOG=info
